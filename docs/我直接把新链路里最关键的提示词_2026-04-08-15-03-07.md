我直接把新链路里最关键的提示词补齐成可落地版本，并尽量按你现有 Topic Card / Reviewed Topic Card 的输入风格来写，这样你接入成本最低。

可以。你现在最需要补的就是新主路径里的这 3 个提示词：

1. `TOPIC_TO_THEME_FAMILY_PROMPT`
2. `BATCH_TOPIC_TO_THEME_FAMILY_PROMPT`
3. `SUBTHEME_TO_BRIEF_PROMPT`

原因很简单：你当前正式链路还是 `review -> 直接 generate_briefs()`，只给 `Approve` 生成单份 brief。tiktok_topic_to_brief_pipeline

 tiktok_topic_to_brief_pipeline

而且当前 `build_reviewed_card(row)` 也是围绕单个：

* `normalized_theme`
* `theme_type`
* `pack_archetype`
* `best_platform`
* `visual_symbols`
* `emotional_hooks`

来组织输入。tiktok_topic_to_brief_pipeline

所以新 prompt 的目标，就是把这套单主题 review 卡，升级成“主题家族 + 子 brief”链路。

我建议你对**新模块统一改成 JSON-only 输出**。

因为你现在旧链路里单条评审和 brief 解析都用了比较复杂的 `_split_parts()`、`_extract_kv()` 解析；新模块直接用 JSON 会省掉很多解析成本。文档里你现在批量评审就已经是 JSON array-only，这种方式最适合新增模块。tiktok_topic_to_brief_pipeline

---

# 1. TOPIC_TO_THEME_FAMILY_PROMPT

这个 prompt 接在 `Topic Review` 后面。

输入是一条 **Reviewed Topic Card**。

输出是一个 **Theme Family**，里面有 3-6 个互相关联的子题材方向。

---

# 2. BATCH_TOPIC_TO_THEME_FAMILY_PROMPT

这个是批量版。

如果你后面想像现在 `BATCH_TOPIC_REVIEW_PROMPT` 一样批量扩 family，这个版本能直接上。

输入是多条 `Reviewed Topic Card`，用 `---TOPIC---` 分隔。

输出是 JSON array，顺序必须和输入一致。

---

# 3. SUBTHEME_TO_BRIEF_PROMPT

这是新主路径里最重要的 prompt。

它替代当前 `TOPIC_TO_BRIEF_PROMPT` 的主职责。

当前你原版 `TOPIC_TO_BRIEF_PROMPT` 是把一个 reviewed topic card 直接变成一份标准化 brief。tiktok_topic_to_brief_pipeline

新版本要改成：

## 用 “父话题 + 子题材 + 人工配量” 生成单个子题材 brief

这样你后面 planner 接到的 brief，就天然带有：

* 这个 brief 属于哪个 parent topic
* 它在 family 里的角色
* 目标张数
* 优先级

---

# 4. 推荐的输入模板

为了让这几个 prompt 真能接入你现有代码，我建议输入也统一格式。

---

## A. `TOPIC_TO_THEME_FAMILY_PROMPT` 的 user message 模板

直接复用你现在的 `build_reviewed_card(row)` 输出，再补一个 family task 标记。

---

## B. `SUBTHEME_TO_BRIEF_PROMPT` 的 user message 模板

这个建议由三段组成：

---

# 5. 你现有链路里怎么替换

你现在的文档主路径里是：

其中 `generate_briefs()` 只对 `Approve` 跑 `TOPIC_TO_BRIEF_PROMPT`。tiktok_topic_to_brief_pipeline

新链路改成：

同时：

* `TOPIC_TO_BRIEF_PROMPT` 不删
* 只保留给老数据兼容和人工强制补单 brief

---

# 6. 最后给你的接入建议

为了最小改动上线，我建议你按这个顺序：

第一步，先接 `TOPIC_TO_THEME_FAMILY_PROMPT`，哪怕 family 先只存在 JSON 字段里，不急着建表。

第二步，工作台先做最简 allocation，只有：

* selected
* priority
* target_sticker_count

第三步，再接 `SUBTHEME_TO_BRIEF_PROMPT`，把 selected subtheme 转成新 brief。

这样你就能很快从：

**一个话题一个 brief**

切到：

## **一个话题多个相关 brief，并且每个 brief 的数量由人控制**

如果你要，我下一条直接继续补：

## **这三个 prompt 对应的 Python 解析 schema 和返回字段校验规则**